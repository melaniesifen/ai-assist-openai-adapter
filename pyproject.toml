[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-assist-openai-adapter"
version = "0.1.0"
description = "OpenAI provider adapter package for the AI Assist Platform."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
